import Loader from "@/components/Loader";
import { FlexContainer } from "./styles";

const Loading = () => {
  return (
    <FlexContainer>
      <Loader />
    </FlexContainer>
  );
};

export default Loading;
