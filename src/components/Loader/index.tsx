import Spinner from "../Spinner";
import { SpinnerContainer } from "./styles";

const Loader = () => {
  return (
    <SpinnerContainer>
      <Spinner />
    </SpinnerContainer>
  );
};

export default Loader;
